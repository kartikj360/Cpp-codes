#include<iostream>
 using namespace std;
 int merge(int arr2[],)
 int main(int argc, char const *argv[])
 {
     int size;
    cout<<"Enter the size of the array";
    cin>>size;
    int arr[size];
    cout<<"Let's input the data in the array";
    for(int i=0;i,size;i++){
        cout<<"Enter the value in the";
        cin>>arr[i];

    }
    cout<<"Your array is";
    for(int j=0;j<size;j++)
    cout<<arr[j]<<"\t";
    cout<<"Now lets sort the array by merge sort";\


     /* code */
     return 0;
 }
 
 int merge(arr[],r,l){

 }